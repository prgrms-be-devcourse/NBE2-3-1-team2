<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.project01.mapper.PurchaseMapper">
    <select id="lastPurchase" resultType="pto">
        SELECT * FROM purchase ORDER BY pid DESC LIMIT 1;
    </select>
    <insert id="insertPurchase" parameterType="pto">
        INSERT INTO purchase (cid, ot, sst, zip, addr, st)
        VALUES (#{cid}, #{ot}, #{sst}, #{zip}, #{addr}, #{st});
    </insert>
    <update id="updatePurchase" parameterType="int">
        UPDATE purchase SET st=-1 WHERE pid=#{pid};
    </update>
    <update id="updateStatus">
        UPDATE purchase SET st=2 WHERE st>0;
    </update>
</mapper>